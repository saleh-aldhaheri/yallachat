@props(['title' => null])

<div class="col-span-full flex items-center gap-3 mb-4 mt-2 text-[10.5px] text-ink-muted font-mono font-semibold uppercase tracking-widest">
    {{ $title }}
    <span class="flex-1 h-px bg-border/60"></span>
</div>
